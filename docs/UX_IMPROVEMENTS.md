# Stellar 人性化改造清单

> 原则：止血优先，不碰架构；每条可独立交付；不做"以后可能有用"的东西。
> 状态：`[ ]` 待做 / `[x]` 已完成 / `[-]` 砍掉（写明原因）。

## P0：正在丢信任（先做这 4 件）

### 1. 空状态说人话（绝不静默空白）
- [ ] 封装全局 `EmptyState` 规范：三种模板——等采集中（带倒计时）/ 采不到（原因+排查入口）/ 真没有（创建入口）
- [x] dashboard 磁盘/CPU/内存（首个落地）
- [ ] 扫全站 `*ngIf="... != null"` 隐藏逻辑，凡是"没数据就空白"的全部替换为 EmptyState
- [ ] 第一批：dashboard 磁盘/CPU/内存、overview 图表、profiles 空列表、审计日志空结果

### 2. 危险操作确认一致 + 软删除
- [ ] 清单：删集群 / 删单条历史 / 清空历史 / 删黑名单 / kill 会话 / 删用户 / 删角色——逐个确认有确认框
- [ ] 确认框写清后果（删多少条、影响谁），不用"确定吗？"
- [-] 软删除整体砍掉：黑名单/MV 是 StarRocks 侧对象，软删除复杂危险；历史记录恢复价值低。确认框已全覆盖，足够。
- [x] 全站审计：仅缺历史单条删除，已补（其余 dashboard/会话/组织/黑名单/MV/用户/角色均有确认）

### 3. 连接失败排查清单
- [x] 后端四分类文案追加排查动作（telnet/安全组/权限/网络互通）
- [-] 一键复制授权 SQL：后端没返回具体缺的权限，做不了，砍掉
- [ ] 权限不足时给"一键复制授权 SQL"按钮（复用 `scripts/permissions` 现有脚本内容）
- [x] 测试按钮旁显示上次成功时间（本轮弹窗内有效）

### 4. 长任务进度感
- [x] 测试连接 testing 时旁边出现 × 可取消（spinner 吞点击，走独立按钮）
- [x] 图标保存按钮 saving/submitted 时转圈（全局 .spin）
- [-] “预计 X 秒”：时长不可预测，写了就是骗人，砍掉
- [x] 保存中统一转圈图标（cluster-form/user-settings，其余文字按钮保持“保存中”文案）

## P1：效率翻倍（P0 完成后按序做）

### 5. 新手 Setup Wizard
- [x] 空集群三步向导（就地指引+添加按钮，不做多步 wizard 组件）
- [-] 健康分：卡片上已有运行状态/节点数，重复，砍掉

### 6. 查询结果导出 CSV（已存在，仅图标化导出按钮）
- [x] 已存在（前端内存生成 CSV，queryLimit 限制行数不爆内存）；导出按钮图标化
- [-] 后端流式导出：前端内存方案够用，砍掉

### 7. Profile 诊断说人话
- [ ] 每条诊断三段式：现象（白话）→ 影响（慢多少）→ 怎么做（一键复制 SQL/参数）
- [ ] 规则编号（S001 等）折叠为详情，不放标题

### 8. 告警订阅（最小版）
- [ ] 只做三条硬编码规则：节点宕机 / 磁盘>85% / P99>阈值；只做站内铃铛通知
- [ ] webhook/飞书/自定义规则一律不做（等有人要再说）

### 9. 平台操作审计
- [ ] 一张表：谁、何时、做了什么（只记增删改，不记查询）；系统管理下加"操作日志"页
- [ ] 保留 90 天，超时自动清（复用 retention 机制）

### 10. 筛选条件 URL 参数化
- [ ] 审计日志 / 会话 / 历史的筛选条件进 URL（复制链接即复现视图）
- [ ] 不做"命名保存筛选器"（使用率低，等有人要再说）

## P2：精致感（有空再做，不承诺）

- [ ] `?` 快捷键一览 + `⌘K` 全局搜索（跳页面/跳集群/搜历史）
- [ ] 表格密度切换（舒适/紧凑）+ 列显隐
- [ ] 跟随系统主题（`prefers-color-scheme`）
- [ ] 面包屑（只做集群详情链路）
- [ ] header 集群选择器加"最近 3 个"

## 明确不做的（防过度设计）

- [-] Excel 原生导出（CSV 够用）
- [-] 自定义告警规则引擎（先三条硬编码）
- [-] 集群回收站（恢复代价大，确认框够了）
- [-] 命名保存筛选器（URL 分享覆盖 90% 场景）
- [-] 飞书/钉钉/短信通知（站内信先行）
- [-] 新手视频教程/产品 tour（wizard 够了）

---

## 落地记录

| 日期 | 内容 | 提交 |
|---|---|---|
| 2026-09-16 | 建清单 | - |
| 2026-09-16 | P0-1 dashboard 空状态 + 空快照 guard | 见提交 |
